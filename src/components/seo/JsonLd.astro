---
import { siteConfig } from '../../data/siteConfig';

const schemaData = {
  '@context': 'https://schema.org',
  '@type': ['Locksmith', 'AutoRepair'],
  name: siteConfig.name,
  legalName: siteConfig.companyName,
  alternateName: siteConfig.tradeName,
  taxID: siteConfig.cnpj,
  description: siteConfig.tagline,
  url: Astro.url.href,
  telephone: siteConfig.phoneRaw,
  priceRange: '$$',
  image: new URL(`${import.meta.env.BASE_URL}images/logo.png`, Astro.site || 'https://heli2047-source.github.io').href,
  address: {
    '@type': 'PostalAddress',
    streetAddress: 'Av. General Carneiro, 411',
    addressLocality: siteConfig.city,
    addressRegion: siteConfig.state,
    addressCountry: 'BR',
  },
  geo: {
    '@type': 'GeoCoordinates',
    latitude: -22.9056,
    longitude: -47.0608,
  },
  openingHoursSpecification: [
    {
      '@type': 'OpeningHoursSpecification',
      dayOfWeek: [
        'Monday',
        'Tuesday',
        'Wednesday',
        'Thursday',
        'Friday',
        'Saturday',
        'Sunday',
      ],
      opens: '00:00',
      closes: '23:59',
    },
  ],
  areaServed: [
    ...siteConfig.coverage.neighborhoods.map((n) => ({
      '@type': 'AdministrativeArea',
      name: `${n}, Campinas - SP`,
    })),
    ...siteConfig.coverage.metroCities.map((c) => ({
      '@type': 'City',
      name: `${c} - SP`,
    })),
  ],
  paymentAccepted: 'Cash, Credit Card, Debit Card, Pix',
  currenciesAccepted: 'BRL',
  aggregateRating: {
    '@type': 'AggregateRating',
    ratingValue: siteConfig.rating.score,
    reviewCount: siteConfig.rating.count,
    bestRating: '5',
    worstRating: '1',
  },
};
---

<script is:inline type="application/ld+json" set:html={JSON.stringify(schemaData)} />
