---
import { siteConfig } from '../data/siteConfig';
---

<section id="servicos" class="py-12 sm:py-16 bg-rescue-bg-card border-b border-rescue-border">
  <div class="max-w-6xl mx-auto px-4 sm:px-6">
    <!-- Cabeçalho da Seção -->
    <div class="text-center max-w-2xl mx-auto mb-10">
      <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-rescue-red/10 border border-rescue-red/30 text-rescue-red-light text-xs font-bold uppercase tracking-wider mb-3">
        🔧 Soluções Imediatas 24 Horas
      </div>
      <h2 class="text-2xl sm:text-4xl font-black text-white tracking-tight">
        Qual é a Sua Emergência Agora?
      </h2>
      <p class="mt-2 text-sm sm:text-base text-rescue-text-muted">
        Nossas vans-oficina móveis estão equipadas para resolver seu problema no local, seja no carro, na residência ou na rodovia.
      </p>
    </div>

    <!-- Grid de Serviços Reais com Foco na Solução da Dor -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-6">
      {siteConfig.services.map((service) => {
        const serviceWhatsappUrl = `https://wa.me/${siteConfig.whatsappNumber}?text=${encodeURIComponent(`Olá Chaveiro Rodrigues! Preciso de socorro para *${service.title}* em Campinas.`)}`;

        return (
          <div class="bg-rescue-surface/60 hover:bg-rescue-surface border border-rescue-border/80 hover:border-rescue-red/60 rounded-2xl p-5 sm:p-6 transition-all flex flex-col justify-between group shadow-sm hover:shadow-lg">
            <div>
              <div class="flex items-center justify-between gap-2 mb-3">
                <span class="text-3xl p-2.5 rounded-xl bg-rescue-bg-elevated border border-rescue-border/80 inline-block group-hover:border-rescue-red/40 transition-colors">
                  {service.icon}
                </span>
                {service.badge && (
                  <span class="px-2.5 py-0.5 rounded-full text-[11px] font-extrabold bg-rescue-red/15 text-rescue-red-light border border-rescue-red/30">
                    {service.badge}
                  </span>
                )}
              </div>

              <h3 class="text-lg font-black text-white group-hover:text-rescue-red-light transition-colors mb-2">
                {service.title}
              </h3>

              <p class="text-xs sm:text-sm text-rescue-text-muted leading-relaxed mb-4">
                {service.shortDesc}
              </p>

              <!-- Lista de Vantagens Rápidas -->
              <ul class="space-y-1.5 mb-6 text-xs text-slate-300">
                {service.features.map((feat) => (
                  <li class="flex items-center gap-2">
                    <span class="text-emerald-400 font-bold">✓</span>
                    <span>{feat}</span>
                  </li>
                ))}
              </ul>
            </div>

            <!-- Botão de Solicitação Direta e Facilitada -->
            <a
              href={serviceWhatsappUrl}
              target="_blank"
              rel="noopener noreferrer"
              class="touch-target w-full py-2.5 px-4 rounded-xl bg-rescue-bg-elevated hover:bg-rescue-whatsapp hover:text-rescue-bg text-white border border-rescue-border hover:border-transparent font-bold text-xs sm:text-sm flex items-center justify-center gap-2 transition-all group-hover:border-rescue-red/30"
              aria-label={`Pedir socorro para ${service.title}`}
            >
              <span>Resolver Esse Problema</span>
              <span class="text-rescue-red-light group-hover:text-rescue-bg font-black">→</span>
            </a>
          </div>
        );
      })}
    </div>
  </div>
</section>
